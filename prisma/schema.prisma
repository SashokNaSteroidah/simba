generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Roles {
  admin
  user
}

model users {
  id       String @id @unique
  name     String @db.VarChar(100)
  password String @db.Char(80)
  role     Roles
}
